diff --git a/node_modules/react-native-vlc-media-player/VLCPlayer.js b/node_modules/react-native-vlc-media-player/VLCPlayer.js
index 9b91442..ded8972 100644
--- a/node_modules/react-native-vlc-media-player/VLCPlayer.js
+++ b/node_modules/react-native-vlc-media-player/VLCPlayer.js
@@ -33,10 +33,13 @@ export default class VLCPlayer extends Component {
   };
 
   setNativeProps(nativeProps) {
-    this._root.setNativeProps(nativeProps);
+    if (this._root && this._root.setNativeProps) {
+      this._root.setNativeProps(nativeProps);
+    }
   }
 
   startRecording(path) {
+    if (!RCTVLCPlayer || !this._root) return;
     UIManager.dispatchViewManagerCommand(
       ReactNative.findNodeHandle(this),
       UIManager.getViewManagerConfig('RCTVLCPlayer').Commands
@@ -46,6 +49,7 @@ export default class VLCPlayer extends Component {
   }
 
   stopRecording() {
+    if (!RCTVLCPlayer || !this._root) return;
     UIManager.dispatchViewManagerCommand(
       ReactNative.findNodeHandle(this),
       UIManager.getViewManagerConfig('RCTVLCPlayer').Commands.stopRecording,
@@ -54,6 +58,7 @@ export default class VLCPlayer extends Component {
   }
 
   stopPlayer() {
+    if (!RCTVLCPlayer || !this._root) return;
     UIManager.dispatchViewManagerCommand(
       ReactNative.findNodeHandle(this),
       UIManager.getViewManagerConfig('RCTVLCPlayer').Commands.stopPlayer,
@@ -62,6 +67,7 @@ export default class VLCPlayer extends Component {
   }
 
   snapshot(path) {
+    if (!RCTVLCPlayer || !this._root) return;
     UIManager.dispatchViewManagerCommand(
       ReactNative.findNodeHandle(this),
       UIManager.getViewManagerConfig('RCTVLCPlayer').Commands.snapshot,
@@ -70,19 +76,29 @@ export default class VLCPlayer extends Component {
   }
 
   seek(pos) {
-    this.setNativeProps({ seek: pos });
+    if (RCTVLCPlayer) {
+      this.setNativeProps({ seek: pos });
+    } else if (this._root && typeof this._root.setPositionAsync === 'function') {
+      this._root.setPositionAsync(pos * 1000);
+    }
   }
 
   resume(isResume) {
-    this.setNativeProps({ resume: isResume });
+    if (RCTVLCPlayer) {
+      this.setNativeProps({ resume: isResume });
+    }
   }
 
   autoAspectRatio(isAuto) {
-    this.setNativeProps({ autoAspectRatio: isAuto });
+    if (RCTVLCPlayer) {
+      this.setNativeProps({ autoAspectRatio: isAuto });
+    }
   }
 
   changeVideoAspectRatio(ratio) {
-    this.setNativeProps({ videoAspectRatio: ratio });
+    if (RCTVLCPlayer) {
+      this.setNativeProps({ videoAspectRatio: ratio });
+    }
   }
 
   _assignRoot(component) {
@@ -227,6 +243,49 @@ export default class VLCPlayer extends Component {
       progressUpdateInterval: this.props.onProgress ? 250 : 0,
     });
 
+    if (!RCTVLCPlayer) {
+      let ExpoVideo = null;
+      try {
+        ExpoVideo = require("expo-av").Video;
+      } catch (e) {}
+
+      if (ExpoVideo && uri) {
+        return (
+          <ExpoVideo
+            ref={this._assignRoot}
+            style={[styles.base, nativeProps.style]}
+            source={{ uri: uri }}
+            shouldPlay={!!(this.props.autoplay && !this.props.paused)}
+            resizeMode="contain"
+            onPlaybackStatusUpdate={(status) => {
+              if (status.isLoaded) {
+                if (status.isPlaying && this._onPlaying) {
+                  this._onPlaying({ nativeEvent: {} });
+                }
+                if (status.isBuffering && this._onBuffering) {
+                  this._onBuffering({ nativeEvent: { isBuffering: true } });
+                }
+                if (status.positionMillis !== undefined && this._onProgress) {
+                  this._onProgress({
+                    nativeEvent: {
+                      currentTime: status.positionMillis,
+                      duration: status.durationMillis || 0,
+                    },
+                  });
+                }
+                if (status.didJustFinish && this._onEnded) {
+                  this._onEnded({ nativeEvent: {} });
+                }
+              } else if (status.error && this._onError) {
+                this._onError({ nativeEvent: {} });
+              }
+            }}
+          />
+        );
+      }
+      return <View ref={this._assignRoot} style={[styles.base, nativeProps.style]} />;
+    }
+
     return <RCTVLCPlayer ref={this._assignRoot} {...nativeProps} />;
   }
 }
@@ -251,6 +310,7 @@ VLCPlayer.propTypes = {
   muted: PropTypes.bool,
   audioTrack: PropTypes.number,
   textTrack: PropTypes.number,
+  videoTrack: PropTypes.number,
   acceptInvalidCertificates: PropTypes.bool,
 
   onVideoLoadStart: PropTypes.func,
@@ -291,4 +351,22 @@ const styles = StyleSheet.create({
     overflow: "hidden",
   },
 });
-const RCTVLCPlayer = requireNativeComponent("RCTVLCPlayer", VLCPlayer);
+const isRCTVLCPlayerAvailable = () => {
+  try {
+    if (UIManager.getViewManagerConfig) {
+      return !!UIManager.getViewManagerConfig("RCTVLCPlayer");
+    }
+    return !!UIManager.RCTVLCPlayer;
+  } catch (e) {
+    return false;
+  }
+};
+
+let RCTVLCPlayer = null;
+try {
+  if (isRCTVLCPlayerAvailable()) {
+    RCTVLCPlayer = requireNativeComponent("RCTVLCPlayer", VLCPlayer);
+  }
+} catch (e) {
+  RCTVLCPlayer = null;
+}
diff --git a/node_modules/react-native-vlc-media-player/android/.classpath b/node_modules/react-native-vlc-media-player/android/.classpath
new file mode 100644
index 0000000..bbe97e5
--- /dev/null
+++ b/node_modules/react-native-vlc-media-player/android/.classpath
@@ -0,0 +1,6 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<classpath>
+	<classpathentry kind="con" path="org.eclipse.jdt.launching.JRE_CONTAINER/org.eclipse.jdt.internal.debug.ui.launcher.StandardVMType/JavaSE-17/"/>
+	<classpathentry kind="con" path="org.eclipse.buildship.core.gradleclasspathcontainer"/>
+	<classpathentry kind="output" path="bin/default"/>
+</classpath>
diff --git a/node_modules/react-native-vlc-media-player/android/.gradle/6.9/fileChanges/last-build.bin b/node_modules/react-native-vlc-media-player/android/.gradle/6.9/fileChanges/last-build.bin
new file mode 100644
index 0000000..f76dd23
Binary files /dev/null and b/node_modules/react-native-vlc-media-player/android/.gradle/6.9/fileChanges/last-build.bin differ
diff --git a/node_modules/react-native-vlc-media-player/android/.gradle/6.9/fileHashes/fileHashes.lock b/node_modules/react-native-vlc-media-player/android/.gradle/6.9/fileHashes/fileHashes.lock
new file mode 100644
index 0000000..7e2cd1d
Binary files /dev/null and b/node_modules/react-native-vlc-media-player/android/.gradle/6.9/fileHashes/fileHashes.lock differ
diff --git a/node_modules/react-native-vlc-media-player/android/.gradle/6.9/gc.properties b/node_modules/react-native-vlc-media-player/android/.gradle/6.9/gc.properties
new file mode 100644
index 0000000..e69de29
diff --git a/node_modules/react-native-vlc-media-player/android/.gradle/checksums/checksums.lock b/node_modules/react-native-vlc-media-player/android/.gradle/checksums/checksums.lock
new file mode 100644
index 0000000..d0cd515
Binary files /dev/null and b/node_modules/react-native-vlc-media-player/android/.gradle/checksums/checksums.lock differ
diff --git a/node_modules/react-native-vlc-media-player/android/.gradle/vcs-1/gc.properties b/node_modules/react-native-vlc-media-player/android/.gradle/vcs-1/gc.properties
new file mode 100644
index 0000000..e69de29
diff --git a/node_modules/react-native-vlc-media-player/android/.project b/node_modules/react-native-vlc-media-player/android/.project
new file mode 100644
index 0000000..8a3680a
--- /dev/null
+++ b/node_modules/react-native-vlc-media-player/android/.project
@@ -0,0 +1,34 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<projectDescription>
+	<name>react-native-vlc-media-player</name>
+	<comment>Project react-native-vlc-media-player created by Buildship.</comment>
+	<projects>
+	</projects>
+	<buildSpec>
+		<buildCommand>
+			<name>org.eclipse.jdt.core.javabuilder</name>
+			<arguments>
+			</arguments>
+		</buildCommand>
+		<buildCommand>
+			<name>org.eclipse.buildship.core.gradleprojectbuilder</name>
+			<arguments>
+			</arguments>
+		</buildCommand>
+	</buildSpec>
+	<natures>
+		<nature>org.eclipse.jdt.core.javanature</nature>
+		<nature>org.eclipse.buildship.core.gradleprojectnature</nature>
+	</natures>
+	<filteredResources>
+		<filter>
+			<id>1779979141917</id>
+			<name></name>
+			<type>30</type>
+			<matcher>
+				<id>org.eclipse.core.resources.regexFilterMatcher</id>
+				<arguments>node_modules|\.git|__CREATED_BY_JAVA_LANGUAGE_SERVER__</arguments>
+			</matcher>
+		</filter>
+	</filteredResources>
+</projectDescription>
diff --git a/node_modules/react-native-vlc-media-player/android/.settings/org.eclipse.buildship.core.prefs b/node_modules/react-native-vlc-media-player/android/.settings/org.eclipse.buildship.core.prefs
new file mode 100644
index 0000000..1675490
--- /dev/null
+++ b/node_modules/react-native-vlc-media-player/android/.settings/org.eclipse.buildship.core.prefs
@@ -0,0 +1,2 @@
+connection.project.dir=../../../android
+eclipse.preferences.version=1
diff --git a/node_modules/react-native-vlc-media-player/android/src/main/java/com/yuanzhou/vlc/vlcplayer/ReactVlcPlayerView.java b/node_modules/react-native-vlc-media-player/android/src/main/java/com/yuanzhou/vlc/vlcplayer/ReactVlcPlayerView.java
index 8c7acdf..0baf18e 100644
--- a/node_modules/react-native-vlc-media-player/android/src/main/java/com/yuanzhou/vlc/vlcplayer/ReactVlcPlayerView.java
+++ b/node_modules/react-native-vlc-media-player/android/src/main/java/com/yuanzhou/vlc/vlcplayer/ReactVlcPlayerView.java
@@ -136,7 +136,13 @@ class ReactVlcPlayerView extends TextureView implements
         if (!isPaused && mMediaPlayer != null) {
             isPaused = true;
             isHostPaused = true;
-            mMediaPlayer.pause();
+            try {
+                if (!mMediaPlayer.isReleased()) {
+                    mMediaPlayer.pause();
+                }
+            } catch (Exception e) {
+                e.printStackTrace();
+            }
             // this.getHolder().setKeepScreenOn(false);
             WritableMap map = Arguments.createMap();
             map.putString("type", "Paused");
@@ -705,6 +711,12 @@ class ReactVlcPlayerView extends TextureView implements
         }
     }
 
+    public void setVideoTrack(int track) {
+        if (mMediaPlayer != null) {
+            mMediaPlayer.setVideoTrack(track);
+        }
+    }
+
     public void startRecording(String recordingPath) {
         if(mMediaPlayer == null) return;
         if(recordingPath != null) {
@@ -827,6 +839,15 @@ class ReactVlcPlayerView extends TextureView implements
             infoHash.append(";");
         }
 
+        if(mMediaPlayer.getVideoTracksCount() > 0) {
+            MediaPlayer.TrackDescription[] videoTracks = mMediaPlayer.getVideoTracks();
+            infoHash.append("videoTracks:");
+            for (MediaPlayer.TrackDescription track : videoTracks) {
+                infoHash.append(track.id).append(":").append(track.name).append(",");
+            }
+            infoHash.append(";");
+        }
+
         Media.VideoTrack video = mMediaPlayer.getCurrentVideoTrack();
         if(video != null) {
             infoHash.append("videoSize:").append(video.width).append("x").append(video.height).append(";");
@@ -864,6 +885,18 @@ class ReactVlcPlayerView extends TextureView implements
                 info.putArray("textTracks", tracks);
             }
 
+            if(mMediaPlayer.getVideoTracksCount() > 0) {
+                MediaPlayer.TrackDescription[] videoTracks = mMediaPlayer.getVideoTracks();
+                WritableArray tracks = new WritableNativeArray();
+                for (MediaPlayer.TrackDescription track : videoTracks) {
+                    WritableMap trackMap = Arguments.createMap();
+                    trackMap.putInt("id", track.id);
+                    trackMap.putString("name", track.name);
+                    tracks.pushMap(trackMap);
+                }
+                info.putArray("videoTracks", tracks);
+            }
+
             Media.VideoTrack video2 = mMediaPlayer.getCurrentVideoTrack();
             if(video2 != null) {
                 WritableMap mapVideoSize = Arguments.createMap();
diff --git a/node_modules/react-native-vlc-media-player/android/src/main/java/com/yuanzhou/vlc/vlcplayer/ReactVlcPlayerViewManager.java b/node_modules/react-native-vlc-media-player/android/src/main/java/com/yuanzhou/vlc/vlcplayer/ReactVlcPlayerViewManager.java
index b935a4d..13d47b9 100644
--- a/node_modules/react-native-vlc-media-player/android/src/main/java/com/yuanzhou/vlc/vlcplayer/ReactVlcPlayerViewManager.java
+++ b/node_modules/react-native-vlc-media-player/android/src/main/java/com/yuanzhou/vlc/vlcplayer/ReactVlcPlayerViewManager.java
@@ -37,6 +37,7 @@ public class ReactVlcPlayerViewManager extends SimpleViewManager<ReactVlcPlayerV
     private static final String PROP_PROGRESS_UPDATE_INTERVAL = "progressUpdateInterval";
     private static final String PROP_TEXT_TRACK = "textTrack";
     private static final String PROP_AUDIO_TRACK = "audioTrack";
+    private static final String PROP_VIDEO_TRACK = "videoTrack";
     private static final String PROP_RECORDING_PATH = "recordingPath";
     private static final String PROP_ACCEPT_INVALID_CERTIFICATES = "acceptInvalidCertificates";
 
@@ -153,6 +154,11 @@ public class ReactVlcPlayerViewManager extends SimpleViewManager<ReactVlcPlayerV
         videoView.setTextTrack(textTrack);
     }
 
+    @ReactProp(name = PROP_VIDEO_TRACK)
+    public void setVideoTrack(final ReactVlcPlayerView videoView, final int videoTrack) {
+        videoView.setVideoTrack(videoTrack);
+    }
+
     @ReactProp(name = PROP_ACCEPT_INVALID_CERTIFICATES, defaultBoolean = false)
     public void setAcceptInvalidCertificates(final ReactVlcPlayerView videoView, final boolean accept) {
         videoView.setAcceptInvalidCertificates(accept);
diff --git a/node_modules/react-native-vlc-media-player/index.d.ts b/node_modules/react-native-vlc-media-player/index.d.ts
index 1a98a6e..2c462fb 100644
--- a/node_modules/react-native-vlc-media-player/index.d.ts
+++ b/node_modules/react-native-vlc-media-player/index.d.ts
@@ -247,6 +247,11 @@ export type VLCPlayerProps = VLCPlayerCallbackProps & {
    */
   textTrack?: number;
 
+  /**
+   * Set videoTrack id (number) (see onLoad callback VideoInfo.videoTracks)
+   */
+  videoTrack?: number;
+
   /**
    * Set to `true` or `false` to allow playing in the background
    * @default false
